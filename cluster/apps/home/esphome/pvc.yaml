---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esph-lab-config-v1
  namespace: home
  labels:
    app.kubernetes.io/name: &name esph-lab
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
