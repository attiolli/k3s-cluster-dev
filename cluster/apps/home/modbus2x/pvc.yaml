---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: modbus2x-lab-config-v1
  namespace: home
  labels:
    app.kubernetes.io/name: &name modbus2x-lab
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: nfs-client
