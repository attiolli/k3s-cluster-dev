---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lms-lab-config-v1
  namespace: home
  labels:
    app.kubernetes.io/name: &name lms-lab
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
