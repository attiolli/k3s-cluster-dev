---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kam-config
  namespace: voice
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs-client-bgp
